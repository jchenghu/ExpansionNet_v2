"""
    This package is a temporary code (dbreaking the "DRY" principle") for ONNX exporting.

    It may replace the codes in models package in the future if everything
    seems fine for both ONNX and non-ONNX deployment.
"""