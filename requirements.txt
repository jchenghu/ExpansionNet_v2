# NOTE: Installing whatever version of torch, torchvision, h5py, Pillow
# fit your machine should work in most cases. Here is reported one possible
# working configuration but these specific versions are not mandatory.

h5py==3.8.0
numpy==1.21.6
Pillow==9.0.1
Pillow==10.2.0
torch==1.12.1+cu113
torchvision==0.10.0+cu111
